# olab 2

Interactive WebGL study with:

- sphere + conical frustum composition
- play modes with organic motion
- frame resize and background image upload
- SVG export (2-shape projected result)

## Run

Open `index.html` directly in a modern browser.
